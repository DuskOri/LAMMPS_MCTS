# LAMMPS_MCTS 开发记录

本文档用于记录 LAMMPS_MCTS 项目的阶段性开发过程，便于后续整理软著材料、项目说明和答辩文档。记录内容按开发时间和功能推进顺序编写，重点描述每个阶段完成的工作、形成的文件和验证结果。

## 2026-07-03 初始聚合物片段构建原型

### 目标

建立项目最早的聚合物片段表示方式，使 MCTS 输出的片段序列能够被转换为 RDKit 可处理的聚合物分子。

### 已完成内容

- 建立 `mcts/Poly_Build.py` 初始版本。
- 定义聚酰亚胺相关片段 SMILES。
- 使用 `[1*]` 和 `[2*]` 表示片段左右连接位点。
- 实现基础片段拼接逻辑。
- 实现聚合物链端基封端逻辑。
- 实现 PDB 文件输出的初步流程。

### 阶段结果

- 项目具备从片段序列生成聚合物链的基础能力。
- 后续 MCTS 搜索结果可以对接该建链模块。

## 2026-07-03 初始片段连接图

### 目标

定义最早的聚合物片段搜索空间，为 MCTS 搜索提供片段连接规则。

### 已完成内容

- 建立 `mcts/graph.py` 初始版本。
- 定义 `Start` 和 `End` 节点。
- 定义聚酰亚胺片段 `A`、`A'`、`B`、`C`。
- 定义片段之间的基础连接关系。

### 阶段结果

- 项目具备最早的聚酰亚胺片段连接图。
- MCTS 后续可以基于该图进行片段序列搜索。

## 2026-07-07 项目接手、梳理与基础修复

### 目标

对已有 LAMMPS_MCTS 项目进行代码检查，在 2026-07-03 已有 `Poly_Build.py` 和 `graph.py` 的基础上，修复影响运行的基础问题，并明确项目后续开发方向。

### 工作内容

- 检查项目目录结构和核心文件。
- 阅读已有的 `mcts/Poly_Build.py` 和 `mcts/graph.py`。
- 修复 `mcts/Poly_Build.py` 中 PDB 保存相关问题。
- 修正 RDKit 三维构象生成流程，增加构象嵌入失败后的随机坐标重试。
- 整理 `requirements.txt`，写入项目运行所需 Python 依赖。

### 阶段结果

- RDKit 建链和 PDB 保存流程可以独立运行。
- 项目具备继续扩展 MCTS 和分子模拟模块的基础。

## 2026-07-07 MCTS 搜索模块完善

### 目标

补全 MCTS 搜索流程，使项目可以从片段连接图出发自动搜索候选聚合物序列。

### 工作内容

- 在已有 `graph.py` 连接图基础上，完善 MCTS 节点、状态、搜索引擎和片段图调用。
- 实现 UCB 选择逻辑。
- 明确 UCB 公式为奖励项与探索项之和。
- 设置搜索根节点为 `Start`。
- 增加 `max_steps`、`iterations`、`top_k`、`exploration_weight` 等配置项。
- 将候选序列按得分排序输出。

### 阶段结果

- MCTS 可以从 `Start` 节点开始搜索。
- 搜索结果可写入 `outputs/candidates.csv`。
- 候选序列可继续传入 RDKit 建链模块。

## 2026-07-07 低热导目标与反馈逻辑

### 目标

将热导率结果作为 MCTS 的反馈，使搜索方向面向低热导聚合物。

### 工作内容

- 明确项目目标为寻找低热导聚合物。
- 将奖励函数方向调整为低热导优先。
- 设计热导反馈奖励：

```text
reward = 1 / (kappa + offset)
```

- 增加启发式低热导评分函数。
- 增加热导反馈评估模块 `mcts/thermal_feedback.py`。
- 将候选数据库按热导率从低到高排序。

### 阶段结果

- MCTS 奖励越高代表热导率越低。
- `low_k_database.csv` 用于保存低热导候选数据库。
- 热导率结果可以反馈到 MCTS 节点。

## 2026-07-07 RDKit 聚合物构建模块扩展

### 目标

在 2026-07-03 已有 `Poly_Build.py` 的基础上，把 MCTS 输出的片段序列稳定转化为可用于后续模拟的聚合物结构。

### 工作内容

- 保留并修复已有 `mcts/Poly_Build.py` 的片段拼接逻辑。
- 完善 `generator/polymer_builder.py`。
- 将 MCTS 片段序列标准化，自动补齐 `End`。
- 将聚合度参数接入构建流程。
- 设置 `degree_of_polymerization`，后续调整为 DP=10。
- 输出候选聚合物的 SMILES 和 PDB 文件。

### 阶段结果

- 候选序列可以转化为聚合物链。
- PDB 文件输出到 `outputs/pdb/`。
- DP=10 时，序列片段会重复 10 次构建长链。

## 2026-07-07 PDB 到 LAMMPS Data 转换

### 目标

实现从 PDB 文件到 LAMMPS data 文件的自动转换，为 LAMMPS 模拟准备输入结构。

### 工作内容

- 新增 `md_engine/lammps_data_writer.py`。
- 解析 PDB 中的原子坐标和连接信息。
- 根据元素类型生成 atom types。
- 根据键连接生成 bond types。
- 输出 `atom_style full` 风格的 LAMMPS data 文件。

### 阶段结果

- 单链 PDB 可以转换为 `candidate_xxx_single.data`。
- Packmol 输出的体系 PDB 可以转换为 `candidate_xxx_system.data`。

## 2026-07-07 Packmol 初始体系生成

### 目标

自动生成多条聚合物链组成的初始模拟体系。

### 工作内容

- 新增 `generator/packmol_runner.py`。
- 编写 Packmol 输入文件。
- 配置 Packmol 可执行文件路径。
- 调用 Packmol 进行多链装盒。
- 将 Packmol 输出体系转换为 LAMMPS data。

### 阶段结果

- `candidate_001` 和 `candidate_002` 成功生成多链初始体系。
- 输出文件包括 Packmol 输入、体系 PDB 和体系 data。
- 对较大候选结构，观察到 Packmol 收敛时间明显增加，后续可通过增大盒子或降低分子数优化。

## 2026-07-07 Packmol 安装与环境配置

### 目标

解决 Windows 环境中 Packmol 不可用的问题。

### 工作内容

- 尝试通过 conda 安装 Packmol。
- 由于 win-64 依赖源不可用，改为源码编译。
- 获取 Packmol 源码。
- 使用 MinGW gfortran 编译生成 `packmol.exe`。
- 将 `packmol.exe` 放入 `polymer_mcts` 环境的 `Scripts` 目录。
- 在 `config.yaml` 中写入 Packmol 路径。

### 阶段结果

- Packmol 可以被主程序调用。
- 项目可以在当前 Windows 机器上自动生成初始体系。

## 2026-07-07 快速热导率 LAMMPS 模块

### 目标

在给定结构基础上生成快速热导率计算输入脚本。

### 工作内容

- 参考用户提供的热导率计算示例。
- 新增 `md_engine/thermal_conductivity_writer.py`。
- 生成 NEMD 风格快速热导率 LAMMPS 输入。
- 设置 hot/cold 区域。
- 输出热流、温度剖面和轨迹文件。
- 默认忽略分子间作用力，使用 `pair_style zero`。
- 增加 `lammps_runner.py` 用于自动运行 LAMMPS。

### 阶段结果

- 成功生成 `candidate_001_fast_tc.in` 和 `candidate_002_fast_tc.in`。
- 手动运行 `candidate_001_fast_tc.in` 成功完成 LAMMPS 热导计算。
- 生成 `flux.profile`、`temp.profile` 和 `dump.lammpstrj`。

## 2026-07-07 热导率后处理与数据库输出

### 目标

解析 LAMMPS 输出结果，形成可排序的低热导候选数据库。

### 工作内容

- 新增 `post_process/thermal_analyzer.py`。
- 读取热流输出文件。
- 读取温度剖面文件。
- 计算热流平均值和温度梯度。
- 估算热导率。
- 新增 `post_process/result_writer.py`，统一写出 CSV。

### 阶段结果

- 输出 `thermal_results.csv`。
- 输出 `low_k_database.csv`。
- 候选按低热导优先排序。

## 2026-07-07 多聚合物片段库扩展

### 目标

使项目不局限于聚酰亚胺，支持更多聚合物体系。

### 工作内容

- 新增 `mcts/fragment_registry.py`。
- 将片段 SMILES、片段家族、片段角色和连接规则集中管理。
- 保留聚酰亚胺片段。
- 增加聚氨酯片段。
- 增加酚醛树脂片段。
- 增加自定义片段入口。
- 修改 MCTS 图读取方式，使搜索空间来自片段库。
- 修改 RDKit 建链模块，使 SMILES 也来自片段库。

### 阶段结果

- 支持 `polyimide`、`polyurethane`、`phenolic` 和 `custom`。
- MCTS 可以在多个聚合物家族之间搜索。
- `Start` 节点可连接多个聚合物体系的起始片段。

## 2026-07-07 自定义片段输入

### 目标

允许用户输入自己的化合物片段，扩展搜索空间。

### 工作内容

- 新增 `custom_fragments.json`。
- 定义自定义片段 JSON 格式。
- 要求片段 SMILES 包含 `[1*]` 和 `[2*]`。
- 支持自定义 transitions 连接图。
- 在主程序中读取自定义片段并合并进片段库。

### 阶段结果

- 用户可通过 JSON 文件添加自己的片段。
- 自定义片段可参与 MCTS 搜索和 RDKit 建链。

## 2026-07-07 AI API 片段生成接口

### 目标

增加人工智能接口，使 AI 可以自动生成片段和连接图。

### 工作内容

- 新增 `mcts/ai_fragment_api.py`。
- 实现 OpenAI-compatible Chat Completions 接口。
- 支持通过环境变量读取 API key。
- 设计 AI 返回 JSON 格式。
- 校验 AI 返回的片段是否包含 `[1*]` 和 `[2*]`。
- 如果 RDKit 可用，进一步校验 SMILES 能否解析。
- 将 AI 输出保存为 `ai_fragments.json`。
- 增加 `ai_fragments.example.json` 示例文件。
- 增加 `AI_FRAGMENT_API.md` 说明文档。
- 在 `config.yaml` 中加入 AI 接口配置。

### 阶段结果

- AI 片段接口默认关闭，不影响离线运行。
- 打开 `ai_enabled` 后，AI 生成片段可以自动进入 MCTS 搜索空间。
- 本地模拟 AI 文件合并测试通过。

## 2026-07-07 README 与说明文档整理

### 目标

完善项目文档，形成可阅读、可交付的说明材料。

### 工作内容

- 重写 `README.md`。
- 增加项目功能说明。
- 增加安装和启动方式。
- 增加 MCTS 低热导奖励说明。
- 增加自定义片段说明。
- 增加 AI 接口说明。
- 增加 Packmol、LAMMPS 和输出文件说明。

### 阶段结果

- README 可作为项目使用说明。
- AI 接口有独立文档。
- 项目当前代码和文档总行数超过 3000 行。

## 2026-07-08 开发留痕整理

### 目标

补充项目开发周期记录，使项目过程更清晰。

### 工作内容

- 整理从基础修复到 AI 接口的阶段性开发过程。
- 按日期记录每个阶段的目标、工作内容和阶段结果。
- 形成 `DEVELOPMENT_LOG.md`。
- 准备同步整理 `CHANGELOG.md`。

### 阶段结果

- 项目具备按时间展开的开发记录。
- 后续可用于软著材料、项目说明和答辩准备。

## 验证记录

开发过程中执行过以下验证：

```text
syntax ok 21
syntax ok 22
syntax ok 23
```

阶段性运行结果包括：

- RDKit 聚合物构建通过。
- Packmol 对部分候选体系装盒通过。
- LAMMPS 快速热导率输入生成通过。
- `candidate_001_fast_tc.in` LAMMPS 运行通过。
- AI 示例片段合并入 MCTS 片段库通过。

## 2026-07-08 轻量前端控制台

### 目标

为项目增加一个简单的可视化入口，使候选序列、热导率结果、片段配置和常用命令可以在浏览器中查看。

### 工作内容

- 新建 `frontend/` 目录。
- 新增 `frontend/index.html`。
- 新增 `frontend/styles.css`。
- 新增 `frontend/app.js`。
- 新增 `web_server.py`，提供本地前端 API。
- 前端读取 `config.yaml`、`outputs/candidates.csv`、`outputs/thermal_results.csv` 等文件。
- 前端支持修改 DP、迭代次数、候选数量和片段空间。
- 前端支持一键启动主流程。
- 前端支持轮询显示当前运行阶段。
- 增加候选序列筛选功能。
- 增加配置摘要展示。
- 增加自定义片段和 AI 片段示例展示。
- 增加主流程、前端服务器和 LAMMPS 命令复制功能。
- 增加有向图配置编辑器。
- 增加 API 生成化合物配置功能。
- 支持将前端配置导出为 `custom_fragments.json` 或 `ai_fragments.json`。
- 增加 `frontend/assets/` 原创 SVG 素材。
- 设计项目标识、聚合物流程图和功能图标。
- 优化首页运行区、阶段区和数据库入口的视觉层次。

### 阶段结果

- 项目具备新手向前端控制台。
- 通过本地服务可以访问 `http://localhost:8010/frontend/`。
- 前端可以通过本地 API 修改常用配置并启动主流程。
- 前端可以人工配置片段连接有向图。
- 前端可以调用 OpenAI-compatible API 生成化合物片段配置。
- 前端视觉风格调整为更偏科研软件的浅色控制台，窄屏下指标卡和导航更紧凑。
- 前端未引入第三方素材，Logo 和图标均为项目内自绘 SVG。

## 2026-07-08 README 使用说明补充

### 目标

补充更完整的使用说明，使初次使用者可以根据 README 独立完成前端启动、参数配置、MCTS 搜索、片段图编辑、AI 片段生成和热导率结果查看。

### 工作内容

- 扩展 `README.md` 中的详细使用说明。
- 增加前端控制台的分步骤使用流程。
- 增加命令行运行流程。
- 增加 DP、MCTS 迭代次数、最大片段步数和反馈模式的调参说明。
- 增加自定义有向图片段输入说明。
- 增加 AI API 片段生成操作说明。
- 增加热导率计算和结果文件查看说明。
- 增加常见问题排查说明。
- 修正 README 目录结构中 `frontend/` 与 `web_server.py` 的展示位置。

### 阶段结果

- README 从功能简介扩展为较完整的用户手册。
- 项目留痕文档记录了本次文档增强工作。
- 后续整理软著材料时，可以直接引用 README 的使用流程和开发记录。

## 2026-07-08 前端视觉风格二次调整

### 目标

参考用户提供的粉紫色侧边栏页面，将前端控制台从普通表单页面调整为更像小型科研工具的页面，同时保留新手向操作入口。

### 工作内容

- 调整 `frontend/index.html` 中的导航分组和首页标题文案。
- 重写 `frontend/styles.css` 的页面主题。
- 将顶部导航在宽屏下调整为左侧导航，在窄屏下自动变为顶部导航。
- 使用粉紫色背景、蓝色主按钮和分组面板增强页面识别度。
- 保留原有 Logo、聚合物流程图和功能图标。
- 收紧页面横向宽度，修复窄屏下的横向滚动问题。
- 验证页面 SVG 资源均可正常加载。
- 验证浏览器控制台无报错。

### 阶段结果

- 前端更接近参考页面的粉紫色工具风格。
- 页面仍保留 DP、迭代次数、有向图、API 生成和结果数据库等原有功能。
- 移动/窄屏显示不再出现页面级横向滚动。

## 2026-07-08 有向图编辑器可视化

### 目标

将前端有向图配置从纯表单和 JSON 输出改为更直观的图形预览，使使用者能直接看到 `Start`、片段节点、`End` 和片段之间的可选连接方向。

### 工作内容

- 在 `frontend/index.html` 的有向图编辑区增加图形预览面板。
- 增加“载入示例”按钮，方便新用户快速理解有向图配置方式。
- 在 `frontend/app.js` 中增加 SVG 绘图逻辑。
- 自动绘制 `Start -> 片段` 的入口边。
- 自动绘制用户配置的片段跳转边和结束边。
- 增加连接边标签列表，便于对照图形和规则。
- 将 JSON 配置折叠到高级查看区域。
- 调整 `frontend/styles.css`，增加有向图画布、节点、箭头和边列表样式。

### 阶段结果

- 有向图页面可以直接显示片段连接图。
- 示例图可生成 5 个节点和多条连接边。
- 页面仍保持原有保存自定义图和 API 片段生成能力。

## 2026-07-10 README 运行指令通用化

### 目标

去除 README 中绑定本机目录的运行命令，使项目上传 GitHub 后，其他用户可以按自己的安装环境配置和运行。

### 工作内容

- 将 `C:\DuskORI\...` 形式的本机绝对路径改为通用命令。
- 将 Python 运行命令统一为 `conda activate polymer_mcts` 后执行 `python main.py`。
- 将前端启动命令统一为 `python web_server.py`。
- 将 LAMMPS 手动运行命令改为 `lmp -in ...`。
- 在 README 中说明 Packmol 和 LAMMPS 可执行文件需要按本机环境写入 `config.yaml`。
- 增加 `PATH` 与绝对路径两种配置方式说明。

### 阶段结果

- README 不再依赖开发机器上的固定安装路径。
- GitHub 用户可以根据自己的 conda、Packmol 和 LAMMPS 安装位置完成配置。
- 项目说明更适合公开仓库和跨机器复现。

## 2026-07-10 前端运行命令通用化

### 目标

修正前端说明页中的运行命令，避免页面继续展示开发机器上的绝对路径，使前端在其他设备上也能作为通用使用说明。

### 工作内容

- 修改 `frontend/app.js` 中的 `renderCommands()`。
- 将前端启动命令改为 `cd LAMMPS_MCTS`、`conda activate polymer_mcts` 和 `python web_server.py`。
- 将主流程命令改为 `python main.py`。
- 将 LAMMPS 手动运行命令改为 `lmp -in outputs/lammps/candidate_001_fast_tc.in`。
- 增加 LAMMPS 需要加入 `PATH` 或在 `config.yaml` 中配置路径的提示。
- 更新 `frontend/index.html` 中的脚本版本号，避免浏览器缓存旧命令。
- 检查前端文件中无 `DuskORI`、`python.exe`、`LAMMPS\bin` 等本机路径残留。

### 阶段结果

- 前端说明页展示的命令可以用于不同设备。
- 本地浏览器验证前端说明页无本机路径残留。
- 页面原有运行、配置、有向图和数据库功能不受影响。

## 2026-07-10 候选序列清除功能

### 目标

为前端数据库页增加数据清理入口，使已有候选序列可以按需清除，避免旧候选和新搜索结果混在一起。

### 工作内容

- 在 `web_server.py` 中新增 `/api/clear-data` 接口。
- 支持清除选中的候选序列。
- 支持清空全部候选序列。
- 清除 CSV 前自动备份到 `outputs/archive/`。
- 在前端数据库页增加候选行复选框。
- 增加“清除选中”和“清空候选”按钮。
- 增加已选候选数量提示。
- 增加危险操作提示样式。
- 更新前端资源版本号。
- 重启本地前端服务并验证清除接口已加载。

### 阶段结果

- 前端可以选择性清除候选序列。
- 清除操作不会删除 PDB、LAMMPS data 或 LAMMPS 输入文件。
- 后端接口在运行中会阻止清理，避免主流程写文件时发生冲突。

## 2026-07-18 主流程复查与搜索语义梳理

### 目标

从程序入口重新核对 MCTS、结构生成、热导评价和结果导出的先后关系，明确迭代次数、候选数量、热导反馈次数以及 reward 的来源，避免搜索阶段和最终批处理阶段职责重复。

### 工作内容

- 逐段检查 `main.py` 的配置读取、片段库配置、MCTS 搜索、结构导出和结果保存逻辑。
- 核对 `feedback_max_evaluations` 与 `iterations` 的关系，明确热导模式下每轮搜索都应获得一次可用 reward。
- 将低热导率确定为唯一优化方向：热导率越低，转换后的 reward 越高，UCB 继续执行最大化选择。
- 确认 `engine.ranked_candidates()` 是主程序实际入口，候选来自搜索阶段已经评价过的完整序列。
- 梳理 `_select()`、`_simulate()`、`backpropagate()` 的职责，以及 rollout 结果如何沿树节点回传。
- 检查原有最终候选二次热导计算流程，确定其与每轮热导反馈重复，形成后续精简方案。
- 对主流程无关的配置读取、说明文本和自定义片段加载辅助函数进行分类，保留实际运行需要的接口。

### 阶段结果

- 搜索目标统一为低热导聚合物，不再混用高热导或相反符号的 reward 解释。
- 明确一次 MCTS 迭代应完成候选生成、热导评价和 reward 回传，而不是先完成全部搜索再统一计算热导。
- 明确最终 `top_k` 只负责导出搜索阶段的最佳候选，不应形成额外的搜索轮次。

## 2026-07-19 MCTS 热导反馈主流程收敛

### 目标

根据当前算法思路，明确把热导率计算作为 MCTS 搜索阶段的 reward 来源。搜索结束后直接取搜索阶段 reward 最高的 `top_k` 候选，不再保留最终批量 LAMMPS 运行和最终热导后处理分支。

### 工作内容

- 调整 `mcts/mcts_engine.py`，记录 rollout 中实际计算过 reward 的完整候选序列。
- `ranked_candidates()` 改为从已评价候选中按 reward 排序返回最终 `top_k`。
- 保留 MCTS thermal feedback 中的 LAMMPS 调用，作为每轮 reward 数据来源。
- 精简 `main.py`，删除最终批量 `run_lammps_jobs()` 和 `analyze_thermal_results()`。
- 精简输出逻辑，`low_k_database.csv` 直接由搜索阶段 `feedback_records` 和最终 `top_k` 候选生成。
- 删除 `config.yaml` 中不再使用的 `lammps.run_enabled`、`lammps.run_final_enabled`、`lammps_runs_csv`、`thermal_results_csv`。
- 清理前端和本地 API 中最终 LAMMPS 批处理阶段的状态展示。
- 更新 README，将热导率说明改为 MCTS 搜索阶段反馈，而不是搜索结束后的批量计算。

### 阶段结果

- 主流程变为：读取配置 -> MCTS 搜索 -> 每轮热导 feedback reward -> 输出 reward 最高的 `top_k` -> 导出结构和输入文件 -> 保存数据库。
- LAMMPS 不再作为最终候选的二次批量评价步骤出现，避免和 MCTS reward 排序逻辑重复。
- `outputs/feedback_records.csv` 和 `outputs/low_k_database.csv` 成为热导反馈和低热导候选排序的核心结果文件。

## 2026-07-19 MCTS、LAMMPS 与候选数据库联调

### 目标

验证热导 reward 确实在每轮搜索中运行并回传，确保前端轮次、候选数据库和最终导出使用同一批搜索结果。

### 工作内容

- 将 thermal 模式的评价上限与本次 MCTS 迭代次数对齐，避免达到旧上限后停止运行 LAMMPS。
- 检查热导评价器的缓存、失败 reward、输入文件路径和反馈记录，区分真实热导结果与失败占位值。
- 使用本地 LAMMPS 对候选输入进行运行验证，记录超时发生阶段、体系原子数和已完成动力学步数。
- 将已评价候选按 reward 从高到低排序，并同步写入低热导候选数据库。
- 明确相同完整序列复用缓存，不重复执行昂贵的 LAMMPS；不同候选仍逐轮评价。
- 核对前端设置的迭代次数与后端实际循环次数，排查“设置两轮却显示三轮”的阶段命名问题。
- 区分 MCTS 循环和搜索结束后的 Top K 结构导出，后者不再计入迭代轮次。

### 阶段结果

- 每一轮 thermal 搜索都从完整候选获得 LAMMPS reward，并回传到当前 MCTS 路径。
- 候选数据库、实时状态和最终 Top K 使用同一套 reward 排序方向。
- 识别出标准 Green-Kubo 参数在单核环境耗时较长，为次日增加快速筛选档和超时配置提供依据。

## 2026-07-20 前端 reward 来源开关

### 目标

在前端运行面板中明确区分两种 MCTS reward 来源：使用 LAMMPS 快速热导率作为 reward，或使用片段结构特征的启发式 reward，避免旧版复选框造成“热导反馈”和“运行 LAMMPS”之间的理解混淆。

### 工作内容

- 将 `frontend/index.html` 中原来的两个复选框改为 reward 来源二选一控件。
- 新增 “LAMMPS 热导 reward” 选项，对应 `feedback_mode: thermal` 和 `feedback_run_lammps: true`。
- 新增 “启发式 reward” 选项，对应 `feedback_mode: heuristic` 和 `feedback_run_lammps: false`。
- 调整 `frontend/app.js` 的配置读取和提交逻辑，使前端保存参数时直接写入 reward 模式。
- 调整 `frontend/styles.css`，为 reward 来源选择器增加简洁样式和移动端单列布局。
- 更新前端静态资源版本号，避免浏览器继续加载旧脚本。

### 阶段结果

- 前端可以直接选择 MCTS 的 reward 来源。
- LAMMPS 是否参与搜索阶段反馈由同一个开关语义控制，不再保留含混的双复选框状态。
- 后端配置仍沿用现有字段，兼容命令行运行和前端运行两种方式。

## 2026-07-20 启发式 roles 前端配置
### 目标

让前端可以明确控制两种 reward 来源：LAMMPS 热导 reward 和启发式 reward。启发式 reward 依赖片段 roles，因此在自定义片段时提供更直观的 roles 配置，并允许用 API 辅助判断输入 SMILES 的 roles。

### 工作内容

- 在运行面板保留 reward 来源二选一开关：`LAMMPS 热导 reward` 或 `启发式 reward`。
- 在有向图编辑器中增加 `启发式 roles` 小面板。
- 增加常用 roles 标签：`rigid`、`aromatic`、`flexible`、`ether`、`imide`、`urethane`、`phenolic` 等。
- 保留手写 roles 输入框，方便直接输入逗号分隔标签。
- 增加“按 SMILES 粗判”按钮，前端可根据芳香环、醚键、酰亚胺、氨基甲酸酯等字符串特征给出初步 roles。
- 增加“用 API 判断 roles”按钮，使用当前页面填写的 API URL、Model 和 API Key 判断单个片段的 roles。
- 调整 API 生成片段的提示词，要求生成结果中包含 `roles` 字段。
- 增加“应用到有向图”按钮，可以把 API 生成的片段、连接关系和 roles 直接放入有向图编辑器。
- 对 API 返回的 roles 做归一化，只保留启发式算法当前识别的标签，避免无效标签影响评分解释。
- 更新前端资源版本号，避免浏览器继续加载旧脚本。

### 阶段结果

- 自定义片段可以在前端完成 key、SMILES、family、roles 和连接关系配置。
- 启发式 reward 模式下，用户不用手写全部 roles，可以通过标签、本地粗判或 API 自动判断补齐。
- LAMMPS reward 模式仍以热导率反馈为主，roles 可作为备用说明和启发式 fallback 的输入。

## 2026-07-20 探索权重前端入口
### 目标

将 MCTS 的 `exploration_weight` 暴露到前端基础参数区，方便使用者直接调整 UCB 公式中的探索项权重。默认值保持为根号二近似值 `1.41421356237`。

### 工作内容

- 在前端基础参数面板增加“探索权重”输入框。
- 默认值设置为 `1.41421356237`，对应常用的 `sqrt(2)`。
- 在 `frontend/app.js` 中增加配置读取和提交字段。
- 在 `web_server.py` 的 `/api/config` 中增加 `exploration_weight` 的读写逻辑。
- 更新前端资源版本号，避免浏览器缓存旧脚本。

### 阶段结果

- 前端可以直接修改 MCTS 探索权重。
- 保存参数或一键运行时，探索权重会写回 `config.yaml` 并参与后续搜索。

## 2026-07-20 移除“全部内置”搜索选项

### 目标

避免使用者误以为三类内置聚合物片段可以作为同一个化学体系组合搜索。前端改为每次明确选择一个聚合物家族，自定义体系继续通过片段图单独配置。

### 工作内容

- 删除前端片段空间选择框中的“全部内置”选项。
- 将前端、服务端和命令行默认家族统一改为聚酰亚胺。
- 读取旧版三家族配置时，前端自动回落到聚酰亚胺，避免选择框为空。
- 更新 README 中的片段空间选择说明和配置示例。
- 更新前端静态资源版本号，避免浏览器继续使用旧页面缓存。

### 阶段结果

- 一次搜索只选择聚酰亚胺、聚氨酯、酚醛树脂或自定义片段中的一种体系。
- 默认启动时只加载聚酰亚胺内置片段和对应连接规则。

## 2026-07-20 按文献修正重复单元与快速 Green-Kubo 流程

### 目标

依据《Autonomous Search for Polymers with High Thermal Conductivity Using a Rapid Green-Kubo Estimation》调整搜索和热导评价语义。MCTS 从 Start 到 End 构建一个重复单元，LAMMPS 保留分子间作用，只在热流相关函数中省略不同分子之间的交叉项。本阶段暂不增加聚合物家族官能团检查，候选结构留待人工筛选。

### 工作内容

- 将 `max_steps` 明确为一个重复单元中的真实片段上限，默认由 6 改为 5。
- `Start` 和 `End` 不计入片段数量；达到上限后状态机强制选择 `End`。
- 保留提前选择 `End` 的能力，因此两片段重复单元仍可直接进入评价。
- 删除冷热端温度梯度 NEMD 输入和 `pair_style zero`、零键力、分子间排除设置。
- 新增按 molecule ID 分组的热流计算，使用 `fix ave/correlate type auto` 只累计分子自身三个方向的热流自相关。
- 新增 NVT、NPT 平衡和 NVE Green-Kubo 采样流程。
- 新增力场完整性检查。data 缺少 Pair、Bond、Angle、Dihedral 或 Improper 系数时停止热导评价并记录缺失项。
- 新增 `uff_screening` 参数化路径：保留 RDKit MOL 键级，计算 Gasteiger 电荷，并将 UFF 非键、键、键角和二面角参数复制到 Packmol 多链体系。
- 将低热导 reward 改为 `1 / (1 + kappa / reward_scale)`，默认尺度为 0.20 W/(m K)，使奖励保持在 0 到 1。
- 关闭 thermal 模式失败后自动回落启发式 reward 的默认行为，避免数据库混合两种来源却仍标记为热导反馈。
- 更新前端重复单元参数名称、默认值、静态资源版本和 README 使用说明。
- 新增重复单元状态机、力场检查、GK 输入特征和热导末段平均测试。

### 阶段结果

- MCTS 当前搜索对象是重复单元，不是整条 DP 链。
- 家族化学规则尚未自动判断，仍需人工复核连接与可合成性。
- 默认 `uff_screening` 已能生成带力场系数的 data，并完成 A–B 重复单元从构建、Packmol 装箱、LAMMPS 运行到热导读取的冒烟测试。
- UFF 结果只作为快速筛选反馈，不等同于论文中的 GAFF 定量结果；后续正式计算仍应接入 GAFF/AM1-BCC 或经验证的聚合物力场。
- `topology_only` 模式仍然保留，但会被热导输入检查明确拒绝，避免输出没有物理意义的零力热导率。

## 2026-07-20 前端控制台视觉重构

### 目标

在保留搜索配置、运行进度、片段编辑、API 生成、结果数据库和候选清理功能的前提下，重新整理前端的信息层级和视觉风格，使页面更接近简洁、稳定的科研工作台。

### 工作内容

- 安装并参考 `emilkowalski/skills` 的设计工程、动画审查和 Apple 界面设计技能。
- 将原有粉色展示型页面改为中性灰背景、白色工作面板和绿色主操作的实验控制台。
- 重构侧边导航、顶部运行栏、页面标题、搜索设置、运行阶段和结果区域。
- 把参数表单整理为固定网格，突出 DP、迭代次数、探索权重、片段上限和 reward 来源。
- 将运行状态改为清晰的就绪、运行中、完成和异常四种反馈，运行期间按钮显示忙碌状态并禁止重复提交。
- 重排片段编辑器、路径预览和 API 生成区域，保留原有 53 个前端元素 ID 和全部后端接口契约。
- 优化数据库工具栏、候选选择、危险操作和横向宽表显示。
- 所有常用按钮增加轻微按压反馈，交互动画限制在 140 毫秒左右，并提供 `prefers-reduced-motion` 兼容。
- 增加 1120、860 和 620 像素三档响应式布局，移动端导航、表单和结果指标改为单列显示。
- 更新静态资源版本号，避免浏览器继续读取旧版 CSS 和 JavaScript。

### 阶段结果

- 桌面端搜索页、片段空间和数据库页均完成浏览器截图检查。
- 390 x 844 移动端视口没有页面级横向溢出，数据表在自身容器内横向滚动。
- JavaScript 语法检查通过，所有 `getElementById` 引用均能在 HTML 中找到对应元素。
- 前端仍使用原生 HTML、CSS 和 JavaScript，没有增加构建工具或运行依赖。

## 2026-07-20 目标链长与自动聚合度

### 目标

取消运行界面中的手动 DP 参数。参考论文约 `120 Å` 的聚合物链长度设置，根据每个候选重复单元的轮廓长度自动计算实际聚合度，使不同尺寸的重复单元能够生成尺度接近的聚合物链。

### 工作内容

- 将 `polymer.degree_of_polymerization` 替换为 `target_chain_length_angstrom`，默认值为 `120.0`。
- 增加自动 DP 上下限配置，默认范围为 1 到 100。
- 从未封端重复单元中识别 `[1*]` 和 `[2*]` 两个连接位点。
- 沿两个连接位点邻接原子之间的最短化学键路径计算内部轮廓长度。
- 优先使用 RDKit UFF 平衡键长；缺少 UFF 参数时使用元素共价半径之和作为回退。
- 把重复单元内部路径和单元间连接键共同计入整链长度，选择最接近目标值的整数 DP。
- 主流程最终结构导出和 MCTS 每轮热导反馈统一使用自动聚合度。
- 前端将“聚合度 DP”输入改为“目标链长 (Å)”，实际 DP 不再由使用者填写。
- `build_results.csv` 新增 DP 模式、目标长度、重复单元长度和估算链长字段。
- 保留构建函数显式传入 DP 的兼容入口，只用于旧代码或单元测试；项目默认流程不再使用手动 DP。

### 阶段结果

- A–B 重复单元的轮廓增量估算为 `12.7187 Å`。
- 目标 `120 Å` 时自动选择 DP=10，估算整链轮廓长度为 `125.6667 Å`。
- A–B 自动长度结构已完成完整 MOL/PDB 构建验证。
- 自动 DP、真实重复单元长度、状态机、UFF data 和 Green-Kubo 输入等 8 项测试全部通过。
- 当前长度是沿化学键路径的轮廓长度，不是折叠后三维构象的端到端直线距离。

## 2026-07-20 前端热导反馈阶段顺序修正

### 目标

修正运行面板中热导反馈显示在建链之前的问题，使前端阶段顺序与 MCTS 热导 reward 的实际计算流程一致。

### 工作内容

- 将阶段顺序调整为 MCTS 选择、RDKit 建链、Packmol 装箱、LAMMPS 输入、LAMMPS 计算、reward 回传和保存结果。
- 根据热导评价器已有的进度消息识别本轮正在执行的具体步骤。
- 保持后端计算和正在运行的任务不变，仅修正前端状态展示。

### 阶段结果

- 前端不再造成“未建链便获得热导反馈”的误解。
- 每轮完成 LAMMPS 热导计算后，界面才进入 reward 回传阶段。

## 2026-07-20 MCTS 循环进度与实时 Top K

### 目标

让前端能够反映 MCTS 评价流程会重复执行，并在搜索尚未结束时显示当前轮次和已评价候选排名。

### 工作内容

- MCTS 引擎在每轮选择开始前上报当前轮次和总迭代次数。
- 每次 reward 返回后上报候选序列、分数、访问次数和最近评价轮次。
- 热导评价完成后补充热导率、成功状态和输入文件等实时信息。
- 服务端按序列合并重复候选，并按 reward 从高到低维护运行期榜单。
- 前端增加循环进度条、已评价数量和实时 Top K 表格。
- 启发式 reward 与 LAMMPS 热导 reward 共用同一套实时榜单事件。

### 阶段结果

- 使用者可以看到当前执行到第几轮，不再把阶段列表理解为单次执行。
- 新候选完成评价后无需等待 CSV 导出即可进入实时榜单。
- 重复候选只更新访问次数和分数，不会在榜单中产生重复行。

## 2026-07-20 空闲状态迭代目标同步

- 修正前端修改迭代次数后，循环进度仍显示旧配置值的问题。
- 未运行时直接跟随迭代输入框显示 `0/N`；运行后切换为服务端实际轮次。
- 该调整只影响界面状态同步，不改变 MCTS 搜索参数和计算逻辑。

## 2026-07-20 迭代尾声与片段空间修正

- 核对运行日志，确认设置两轮时 MCTS 和 LAMMPS reward 只执行两轮。
- 将搜索结束后的结构、Packmol 体系和 LAMMPS 输入整理单独标记为“导出最终 Top K”，避免被误认为额外一轮搜索。
- 修正自定义片段无条件加入 `Start` 的问题；只有选择 `custom` 搜索空间时才加载自定义片段。
- 增加内置家族与自定义片段隔离测试。

## 2026-07-20 Green-Kubo 快速筛选档

- 根据本机 LAMMPS 日志确认原标准参数在单核下每个候选约需 1 到 2 小时。
- 增加快速筛选和标准复核两套参数档位，并在前端提供选择入口。
- 快速档使用 4 条聚合物链、约 40000 个动力学步和 30 分钟超时，用于 MCTS 初筛。
- 标准档保留 10 条链和约 160000 个动力学步，用于最终候选复算。
- 快速档降低统计精度，不作为最终定量热导率结果。

## 2026-07-20 LAMMPS 超时前端配置

- 在搜索设置中增加 LAMMPS 超时秒数输入框。
- 快速筛选建议值为 1800 秒，标准复核建议值为 7200 秒。
- 切换 Green-Kubo 档位时自动填写对应建议值，仍允许人工覆盖。
- 输入 0 时向 LAMMPS 运行器传入无限时设置。

## 当前行数统计

最近一次统计结果：

```text
总行数：9590
Python：4406
Markdown：1842
HTML：411
CSS：1629
JavaScript：1122
SVG：46
JSON：42
YAML：90
TXT：2
```

## 2026-07-20 MCTS 未使用接口清理

- 删除主程序未调用的 `MCTSEngine.search()`，统一通过 `ranked_candidates()` 返回实际评价过的完整候选。
- 删除仅供旧 `search()` 使用的 `_best_terminal_sequence()` 和 `MCTSNode.most_visited_child()`。
- 删除不会产生有效候选的 `_collect_candidates()` 兜底逻辑，候选结果只来自 `_simulate()` 实际完成 reward 评价的序列。
- 删除没有调用且已有替代接口的 `is_valid_transition()`、`validate_sequence()`、`current_graph()` 和 `fragment_smiles()`。
- 删除快速 GK 结果对象中未使用的旧字段别名、恒为零的占位属性和 `analyze_thermal_outputs()` 兼容入口。
- 删除未使用的 `batch_extract_features()`，前端结果表同时移除已经停用的热流和温度梯度列。

## 2026-07-20 GitHub 发布整理

- 补全 7 月 18 日至 20 日的主流程审查、MCTS-LAMMPS 联调和界面完善记录。
- 同步更新 `CHANGELOG.md`，按日期概括近期行为变化。
- 发布范围只包含源码、配置、说明文档和测试，不提交 `log.lammps` 与 `outputs/` 中的本地模拟产物。
- 发布前执行 Python 语法检查、单元测试、Git 空白错误检查和前端浏览器检查。
